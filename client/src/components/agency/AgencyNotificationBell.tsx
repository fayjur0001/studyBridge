"use client";

import NotificationBell from "@/components/notifications/NotificationBell";

export default function AgencyNotificationBell({ className = "" }: { className?: string }) {
  return <NotificationBell className={className} />;
}
